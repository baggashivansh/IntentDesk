# IntentDesk
IntentDesk is a generative UI workspace where users express what they want in natural language and the interface adapts instantly. Instead of navigating menus, the UI reshapes itself based on intent. Built using Tambo’s Generative UI SDK for React.
